services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: docker-dev-template
    image: docker-dev-template:local
    security_opt:
      - no-new-privileges:true
    volumes:
      - .:/app
    environment:
      - ENVIRONMENT=development
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=DEBUG
    env_file:
      - .env
    ports:
      - "8000:8000"
    # depends_on:
    #   postgres:
    # healthcheck:
    #   test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health/live')"]    
    #   interval: 60s
    #   timeout: 3s
    #   retries: 3
    #   start_period: 10s
 

  # app-prod:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #     target: production
  #   container_name: docker-dev-template-prod
  #   image: docker-dev-template:prod
  #   profiles: ["prod"]
  #   security_opt:
  #     - no-new-privileges:true
  #   environment:
  #     - ENVIRONMENT=production
  #     - PYTHONUNBUFFERED=1
  #   env_file:
  #     - .env
  #   ports:
  #     - "8001:8000"
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health/live')"]
  #     interval: 30s
  #     timeout: 5s
  #     retries: 3
  #     start_period: 10s
  #   deploy:
  #     resources:    # Resource limits
  #       limits:
  #         cpus: '1.0'
  #         memory: 512M
  #       reservations:
  #         cpus: '0.5'
  #         memory: 256M
  #   depends_on:
  #     - postgres
  #   networks:
  #    - app-network

  # postgres: # Example: add database service
  #   image: postgres:15-alpine
  #   container_name: postgres-db
  #   environment:
  #     POSTGRES_USER: ${POSTGRES_USER:-appuser}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-apppassword}
  #     POSTGRES_DB: ${POSTGRES_DB:-appdb}
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   networks:
  #     - app-network
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-appuser}"]
  #     interval: 30s
  #     timeout: 5s
  #     retries: 5

  
# networks:
#   app-network:
#     driver: bridge

# volumes:
#   postgres-data: